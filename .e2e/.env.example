# Copy to .e2e/.env and fill in. Used by .e2e/local_start_test.sh.
# .e2e/.env is gitignored — do not commit real tokens.

# ClawChat user JWT used to request a one-time agent connect code from
# POST /v1/agents/connect-codes. The "Bearer " prefix is optional;
# the script adds it if missing.
JWT=
